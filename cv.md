# 候选人才画像（CV）

> 本通用版**不再依赖本文件**存放候选人信息。候选人的简历与补充资料请在网页「👤 我的画像」中维护（持久化到浏览器 localStorage），或编辑 `profile/default-profile.json` 作为默认模板。

## 为什么要外置？
个人版把特定候选人的简历写死在 `cv.md` 与 `knowledge/` 中；通用版把这些全部改为**运行时由用户填写的画像**，从而适配任意候选人、任意岗位。

## 你该填什么（在「我的画像」里）
1. **基本信息**：姓名 / 期望城市 / 学历 / 联系方式 / 目标岗位 / 目标行业
2. **简历正文**：完整、真实的简历要点（工作经历含时间、项目、量化成果、能力、诚实标注的短板）
3. **补充资料 / 知识库**：可增删改的真实经历证据（评估与定制据此引用，**绝不编造**）
4. **求职偏好**：硬偏好、风险红线、职业叙事
5. **评分配置**：四维权重（自动归一化）与推荐阈值（默认 4.0）

所有字段均可留空，但建议至少填 `resume` 与 `careerGoal` 后再做真实评分。
